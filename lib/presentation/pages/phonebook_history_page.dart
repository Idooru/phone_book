import 'package:flutter/material.dart';

class PhonebookHistoryPage extends StatelessWidget {
  const PhonebookHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("history page"),
      ),
    );
  }
}
